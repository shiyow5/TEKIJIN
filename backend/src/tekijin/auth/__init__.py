"""Authentication: password hashing, JWT tokens, principals, and FastAPI deps.

The login milestone anticipated by ADR-0005 (the ``require_reader`` seam). See
:mod:`tekijin.auth.deps` for the single place access control is decided.
"""
