# Dev/CI tooling for the backend.
# Runtime dependencies live in requirements.txt.
ruff>=0.6,<1.0
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0

# Local, root-less PostgreSQL (bundles pgvector) so the data-layer tests can run
# without Docker. CI ignores it: it sets TEKIJIN_DATABASE_URL to its own
# pgvector service. Linux/macOS wheels only.
pgserver>=0.1,<1.0 ; sys_platform != "win32"
