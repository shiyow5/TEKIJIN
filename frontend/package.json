{
  "name": "tekijin-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "TEKIJIN frontend — tooling scaffold only (app not implemented yet).",
  "type": "module",
  "scripts": {
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
